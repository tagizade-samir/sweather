enum Routes {
  Home = 'Home',
  AddCity = 'AddCity',
}

enum StorageKeys {
  cities = 'cities',
}

enum ApiRoutes {
  search = 'search',
  forecast = 'forecast',
}

export {Routes, StorageKeys, ApiRoutes}
